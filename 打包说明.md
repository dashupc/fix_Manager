# 简易维修管理系统 - EXE打包说明

## 打包方法

### 方法一：使用批处理脚本（推荐）

1. **确保已创建虚拟环境**
   ```bash
   python -m venv venv
   ```

2. **运行打包脚本**
   ```bash
   build_exe.bat
   ```

3. **打包完成后**
   - 可执行文件位于 `dist\简易维修管理系统.exe`
   - 可以直接运行，无需安装Python

### 方法二：使用spec文件

1. **激活虚拟环境**
   ```bash
   venv\Scripts\activate
   ```

2. **安装依赖**
   ```bash
   pip install -r requirements.txt
   pip install pyinstaller
   ```

3. **使用spec文件打包**
   ```bash
   pyinstaller recovery_manager_app.spec
   ```

### 方法三：直接使用PyInstaller命令

```bash
pyinstaller --name="简易维修管理系统" ^
    --onefile ^
    --windowed ^
    --icon=icon.ico ^
    --add-data="icon.ico;." ^
    --hidden-import=pystray ^
    --hidden-import=PIL ^
    --hidden-import=dateutil ^
    --hidden-import=dateutil.relativedelta ^
    --noconsole ^
    --clean ^
    recovery_manager_app.py
```

## 打包后的文件说明

- **可执行文件**: `dist\简易维修管理系统.exe`
- **文件大小**: 约 20-30 MB（包含所有依赖）
- **运行要求**: Windows 7 或更高版本

## 首次运行说明

1. 双击 `简易维修管理系统.exe` 运行
2. 程序会在exe同目录下自动创建：
   - `recovery_manager.db` - 数据库文件
   - `app.log` - 日志文件
   - `settings.txt` - 设置文件（如果配置了邮箱备份）

## 注意事项

1. **数据库位置**: 数据库文件会在exe所在目录创建，请确保exe有写入权限
2. **图标文件**: 图标已包含在exe中，无需单独放置icon.ico
3. **杀毒软件**: 某些杀毒软件可能会误报，这是PyInstaller打包的常见情况，可以添加信任
4. **文件大小**: 由于包含Python解释器和所有依赖，exe文件较大是正常的

## 常见问题

### Q: 打包时提示缺少模块？
A: 确保已安装所有依赖：`pip install -r requirements.txt`

### Q: 运行时提示找不到icon.ico？
A: 图标已包含在exe中，如果仍有问题，检查spec文件中的datas配置

### Q: 打包后的exe无法运行？
A: 检查是否有杀毒软件拦截，或尝试在命令行运行查看错误信息

### Q: 如何减小exe文件大小？
A: 可以使用UPX压缩（已在spec中启用），或使用--exclude-module排除不需要的模块

## 更新日志

- 2024: 初始打包配置
- 支持Windows系统托盘
- 包含所有必要依赖

